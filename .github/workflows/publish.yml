name: Publish to crates.io

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      dry-run:
        description: "Dry run (don't actually publish)"
        required: false
        default: "false"
        type: boolean

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  publish:
    name: Publish crate
    runs-on: macos-14
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-ios, aarch64-apple-ios-sim

      - name: Cache Cargo registry + git deps
        uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: publish-cargo-${{ hashFiles('Cargo.lock', 'Cargo.toml') }}
          restore-keys: publish-cargo-

      - name: Verify crate version matches release tag
        if: github.event_name == 'release'
        run: |
          CRATE_VERSION="v$(cargo metadata --no-deps --format-version 1 \
            | jq -r '.packages[] | select(.name == "gpui-pre-mobile") | .version')"
          TAG="${{ github.event.release.tag_name }}"
          echo "Crate version: $CRATE_VERSION"
          echo "Release tag:   $TAG"
          if [ "$CRATE_VERSION" != "$TAG" ]; then
            echo "::error::Crate version ($CRATE_VERSION) does not match release tag ($TAG)"
            exit 1
          fi

      - name: Check iOS backends before publishing
        run: |
          cargo check --target aarch64-apple-ios
          cargo check --target aarch64-apple-ios-sim
          cargo check --manifest-path example/Cargo.toml --target aarch64-apple-ios

      - name: Run tests before publishing
        run: cargo test --lib

      - name: Dry-run publish
        if: github.event_name == 'workflow_dispatch' && inputs.dry-run
        run: cargo publish --dry-run --target aarch64-apple-ios
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}

      - name: Publish to crates.io
        if: >-
          (github.event_name == 'release') ||
          (github.event_name == 'workflow_dispatch' && !inputs.dry-run)
        run: cargo publish --target aarch64-apple-ios
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}

      - name: Summary
        if: success()
        run: |
          VERSION=$(cargo metadata --no-deps --format-version 1 \
            | jq -r '.packages[] | select(.name == "gpui-pre-mobile") | .version')
          if [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ "${{ inputs.dry-run }}" = "true" ]; then
            echo "### 🧪 Dry-run publish succeeded" >> "$GITHUB_STEP_SUMMARY"
            echo "Version **$VERSION** is ready to publish." >> "$GITHUB_STEP_SUMMARY"
          else
            echo "### 📦 Published gpui-pre-mobile v$VERSION" >> "$GITHUB_STEP_SUMMARY"
            echo "View on [crates.io](https://crates.io/crates/gpui-pre-mobile)" >> "$GITHUB_STEP_SUMMARY"
          fi
